apiVersion: v2
name: pia
description: A Helm chart for PIA (Project Identity Authority)
type: application
version: 0.2.0
# This chart applies migrations via a pre-upgrade hook job and requires a PIA
# image whose default command is plain uvicorn (i.e. it does NOT migrate on
# startup); that behavior ships in PIA 0.3.1.
appVersion: "0.3.1"
keywords:
  - pia
  - dependency-track
  - eclipse-csi
home: https://github.com/eclipse-csi/pia
sources:
  - https://github.com/eclipse-csi/pia
maintainers:
  - name: eclipse-csi
